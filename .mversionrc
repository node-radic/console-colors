{
    "scripts": {
        "preupdate": "echo 'Bumping version'",
        "postcommit": "npm publish --access public && git push && git push --tags",
        "postupdate": "echo 'Updated to version %s in manifest files'"
    }
}
